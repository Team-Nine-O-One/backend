package com.team901.CapstoneDesign.repository;

import com.team901.CapstoneDesign.entity.OptimizedResultItem;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OptimizedResultItemRepository extends JpaRepository<OptimizedResultItem, Long> {
}